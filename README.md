### Vue.js

- 安装node.js

  - 更换npm源为国内源

  ```shell
  npm install -g cnpm --registry=https://registry.npm.taobao.org
  ```

- 安装Vue-Cli3

  ```shell
  cnpm install -g @vue/cli
  ```

- 在项目文件中

  ```shell
  cnpm install
  ```

- 启动项目

  ```shell
  cnpm run server
  ```

  

